var testUtils = require('./test_modules/test-utils'),    accessTokensMgr = testUtils.accessTokensManager,    modelFilesMgr = testUtils.modelFilesManager,    configMgr = testUtils.configManager;try {    accessTokensMgr.backupCurrentFile();    accessTokensMgr.createNewFile();    modelFilesMgr.backupCurrentFile();    modelFilesMgr.createNewFile();    configMgr.createNewFile();    console.log('Pre test script is now finished');} catch (e) {    console.log(e);}